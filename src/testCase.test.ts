/* const firstCard = require("./testCase");
const secundCard = require("./testCase");
const cardsNumberArray = require("./testCase");

describe("TEST CASE ONE", () => {
    test("firstCard", () => {
        expect(firstCard).toBeNull();
    });

    test("secundCard", () => {
        expect(secundCard).toBeNull();
    });

    test("провекра элемента в массиве", () => {
        expect(cardsNumberArray).toContain("Kk.png");
        expect(new Set(cardsNumberArray)).toContain("Ac.png");
    });
});
 */
